name: CI

on: push

jobs:
  ci:
    name: CI
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

# TODO: Re-enable CI workflow
#     - name: Set up Python 3.11
#       uses: actions/setup-python@v5
#       with:
#         python-version: '3.11'
#         cache: 'pip'

#     - name: Set up Node.js
#       uses: actions/setup-node@v4
#       with:
#         node-version: '22'
#         cache: 'npm'

#     - name: Install dependencies
#       run: |
#         pip install -e ".[dev]"
#         npm ci
#         node_modules/.bin/nx run frontend:install:ci
#         node_modules/.bin/nx run backend:install

#     - name: Lint all projects
#       run: npm run lint

#     - name: Build all projects
#       run: npm run build

#     - name: Verify backend installation
#       run: |
#         python -c "import trip_parser; print(f'Trip Parser version: {trip_parser.__version__}')"
